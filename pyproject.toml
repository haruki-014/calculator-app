[project]
name = "calculator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.2",
    "pytest-cov>=7.1.0",
]
[tool.pytest.ini_options]
pythonpath=["src"]

[dependency-groups]
dev = [
    "pytest-cov>=7.1.0",
    "ruff>=0.15.9",
]
